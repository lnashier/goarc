// Package buildinfo reports build/runtime metadata (host, version, commit
// hash, start time, uptime) as JSON, either directly via Handler or wired
// into an http.Service via App.
package buildinfo
