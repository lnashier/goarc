// Package env reads the process's deployment Environment from the ENV
// environment variable, once, and provides Hostname, a small convenience
// that returns "localhost" for a Local environment.
package env
